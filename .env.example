# Copy to .env (gitignored) and fill in your real key.
# Only needed for `lightning.network = "mainnet"` (server) or
# `--ln-network mainnet` (client) — "regtest" needs no API key.
# Free key: https://breez.technology
BREEZ_API_KEY=